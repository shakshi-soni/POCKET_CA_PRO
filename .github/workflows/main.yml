name: Keep Alive - PocketCA

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -f https://pocketca-f6bt3bbtuf9mhxchtsg8bb.streamlit.app// || echo "Ping failed"
